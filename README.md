Basic proof-of-concept for arranging individual PDFs, then merging to one PDF and downloading.

NOTE: due to cross-origin restrictions for `file://` URLs, PDFs must be served via `http://` when running locally.
